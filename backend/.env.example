PORT=5000
NODE_ENV=development
MONGO_URI=mongodb://localhost:27017/katalyst
JWT_SECRET=replace_with_64_byte_random_hex_run__node_-e_"require('crypto').randomBytes(64).toString('hex')"

# Optional: leave unset to fall back to console-log mock email delivery.
# EMAIL_USER is the sending Gmail address; EMAIL_APP_PASSWORD is a 16-char
# App Password (not the account password) from
# https://myaccount.google.com/apppasswords — requires 2-Step Verification.
EMAIL_USER=
EMAIL_APP_PASSWORD=
